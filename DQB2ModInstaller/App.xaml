<Application x:Class="DQB2ModInstaller.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DQB2ModInstaller"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        
        <!-- Dark Mode Colors -->
        <Color x:Key="DarkBlueColor">#242435</Color>
        <Color x:Key="DarkAccentColor">#BF4B00</Color>
        <Color x:Key="DarkSelectedColor">#FF5500</Color>

        <Color x:Key="TextDataColorBold">#00FFFF</Color>
        <Color x:Key="TextDataColor">#CCFFFF</Color>
        <Color x:Key="FlowDataColorBold">#00FF00</Color>
        <Color x:Key="FlowDataColor">#CCFFCC</Color>

        <SolidColorBrush x:Key="TabItem.MouseOver.Border" Color="{StaticResource DarkBlueColor}"/>
        <SolidColorBrush x:Key="TabItem.Selected.Background" Color="{StaticResource DarkBlueColor}"/>
        <SolidColorBrush x:Key="TabItem.Selected.Border" Color="{StaticResource DarkBlueColor}"/>
        <SolidColorBrush x:Key="TabItem.Disabled.Background" Color="Black"/>
        <SolidColorBrush x:Key="TabItem.Disabled.Border" Color="Black"/>
        <SolidColorBrush x:Key="TabItem.Selected.Foreground" Color="Gold"/>

        <SolidColorBrush x:Key="DarkBlueBrush" Color="{StaticResource DarkBlueColor}" />
        <SolidColorBrush x:Key="DarkOrangeBrush" Color="{StaticResource DarkAccentColor}" />
        <SolidColorBrush x:Key="MediumOrangeBrush" Color="{StaticResource DarkSelectedColor}" />
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#242435"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">

                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="Border" CornerRadius="5" BorderBrush="{StaticResource DarkBlueBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <TextBlock Name="text" Text="{TemplateBinding Content}" Foreground="White" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="Black" />
                                <Setter TargetName="text" Property="Foreground" Value="Gray" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkOrangeBrush}" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource MediumOrangeBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="Gold" />
                                <Setter TargetName="text" Property="Foreground" Value="Gold" />
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource MediumOrangeBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Black" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource DarkBlueBrush}" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness ="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
                                <Border Background="{TemplateBinding BorderBrush}" Height="1" Margin="6,4" VerticalAlignment="Bottom"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover State -->
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="Gold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource MediumOrangeBrush}" />
                            </Trigger>
                            <!-- Disabled State -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="Black" />
                                <Setter Property="Foreground" Value="Gray" />
                                <Setter Property="BorderBrush" Value="Gray" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White" />
        </Style>
    </Application.Resources>
</Application>
